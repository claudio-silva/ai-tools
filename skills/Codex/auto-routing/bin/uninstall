#!/usr/bin/env bash
# Uninstall auto-routing through the repository skill tool.
set -euo pipefail
REPO="$(cd "$(dirname "$0")/../../../.." && pwd)"
exec "$REPO/bin/skills" uninstall auto-routing "$@"
